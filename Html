<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Freelance Developer Portfolio</title>
  <meta name="description" content="Freelance web developer portfolio showcasing projects, skills, and contact." />
  <style>
    :root{
      --bg:#0f1220; --card:#141830; --text:#e8ebff; --muted:#9aa3c7;
      --brand:#6c8cff; --brand-2:#66e0d6; --accent:#ffb86b; --line:#273056;
    }
    *{box-sizing:border-box}
    html{scroll-behavior:smooth}
    body{
      margin:0; font-family:Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      color:var(--text); background:radial-gradient(1200px 800px at 10% -10%, #1a1f3a 0%, #0f1220 55%) no-repeat;
    }
    a{color:inherit; text-decoration:none}
    img{max-width:100%; display:block}

    /* Layout helpers */
    .container{width:min(1100px, 92%); margin-inline:auto}
    .grid{display:grid; gap:1.25rem}
    .btn{
      display:inline-flex; align-items:center; gap:.5rem; padding:.75rem 1rem;
      border-radius:12px; border:1px solid var(--line); background:linear-gradient(180deg,#1a2040, #131935);
      color:var(--text); cursor:pointer; transition:transform .12s ease, box-shadow .2s;
      box-shadow:0 8px 24px rgba(0,0,0,.25);
    }
    .btn:hover{transform:translateY(-1px)}
    .btn.primary{background:linear-gradient(180deg, var(--brand), #516dff); border-color:transparent; color:#fff}
    .pill{display:inline-block; padding:.4rem .7rem; border:1px solid var(--line); border-radius:999px; color:var(--muted); font-size:.85rem}

    /* Header */
    header{
      position:sticky; top:0; z-index:50; backdrop-filter:saturate(160%) blur(10px);
      background:linear-gradient(180deg, rgba(10,12,24,.9), rgba(10,12,24,.55));
      border-bottom:1px solid var(--line);
    }
    nav{display:flex; align-items:center; justify-content:space-between; height:64px}
    .logo{font-weight:800; letter-spacing:.3px}
    .logo span{color:var(--brand)}
    .nav-links{display:flex; gap:1rem; align-items:center}
    .nav-links a{padding:.5rem .7rem; border-radius:10px}
    .nav-links a:hover{background:#1a1f35}
    .hamburger{display:none; border:1px solid var(--line); padding:.5rem .6rem; border-radius:10px; background:#131935}
    @media (max-width:860px){
      .hamburger{display:inline-flex}
      .nav-links{position:fixed; inset:64px 0 auto 0; background:#0f1220; border-bottom:1px solid var(--line);
        flex-direction:column; padding:1rem; display:none}
      .nav-links.show{display:flex}
    }

    /* Hero */
    .hero{padding:96px 0 64px; position:relative; overflow:hidden}
    .orb{
      position:absolute; width:520px; height:520px; right:-160px; top:-140px; filter:blur(70px);
      background:radial-gradient(circle at 40% 40%, var(--brand) 0%, transparent 60%),
                 radial-gradient(circle at 60% 70%, var(--brand-2) 0%, transparent 55%);
      opacity:.25; pointer-events:none;
    }
    .hero-grid{display:grid; grid-template-columns:1.2fr .8fr; gap:2rem; align-items:center}
    @media (max-width:860px){.hero-grid{grid-template-columns:1fr}}
    .headline{font-size:clamp(1.9rem, 4vw, 3rem); line-height:1.1; margin:0 0 .75rem}
    .sub{color:var(--muted); margin:0 0 1.25rem}
    .cta-row{display:flex; gap:.75rem; flex-wrap:wrap}
    .card{
      background:linear-gradient(180deg, #151a35, #12162c); border:1px solid var(--line);
      border-radius:16px; padding:1rem; box-shadow:0 10px 24px rgba(0,0,0,.25)
    }

    /* Sections */
    section{padding:56px 0}
    section h2{font-size:1.6rem; margin:0 0 .25rem}
    section p.lead{color:var(--muted); margin:0 0 1rem}

    /* About */
    .about-wrap{grid-template-columns:1.1fr .9fr}
    @media (max-width:860px){.about-wrap{grid-template-columns:1fr}}

    /* Skills chips */
    .chips{display:flex; gap:.5rem; flex-wrap:wrap}
    .chip{padding:.5rem .7rem; background:#151a35; border:1px solid var(--line); border-radius:12px; font-size:.9rem}

    /* Projects */
    .projects-grid{grid-template-columns:repeat(3, 1fr)}
    @media (max-width:1000px){.projects-grid{grid-template-columns:repeat(2,1fr)}}
    @media (max-width:640px){.projects-grid{grid-template-columns:1fr}}
    .project{
      display:flex; flex-direction:column; gap:.7rem; padding:1rem; border:1px solid var(--line);
      border-radius:16px; background:linear-gradient(180deg,#131936,#0f1430)
    }
    .project h3{margin:.2rem 0}
    .project .tags{display:flex; gap:.4rem; flex-wrap:wrap}
    .project .tags .pill{border-color:#32407a}

    /* Contact */
    form{display:grid; gap:.8rem}
    input, textarea{
      width:100%; padding:.8rem .9rem; border-radius:12px; border:1px solid var(--line);
      background:#121735; color:var(--text); outline:none
    }
    textarea{min-height:140px; resize:vertical}
    .hint{color:var(--muted); font-size:.9rem}

    /* Footer */
    footer{border-top:1px solid var(--line); padding:28px 0 48px; color:var(--muted); text-align:center}

    /* Small motion */
    .reveal{opacity:0; transform:translateY(10px); transition:opacity .6s ease, transform .6s ease}
    .reveal.show{opacity:1; transform:none}
  </style>
</head>
<body>

  <!-- Header -->
  <header>
    <nav class="container">
      <div class="logo">Roja<span>Dev</span></div>
      <button class="hamburger" aria-label="Open menu" id="menuBtn">☰</button>
      <div class="nav-links" id="navLinks">
        <a href="#about">About</a>
        <a href="#skills">Skills</a>
        <a href="#projects">Projects</a>
        <a href="#contact">Contact</a>
        <a class="btn" href="#resume" onclick="alert('Replace with your resume link!')">Resume</a>
      </div>
    </nav>
  </header>

  <!-- Hero -->
  <section class="hero">
    <div class="orb" aria-hidden="true"></div>
    <div class="container hero-grid">
      <div>
        <span class="pill">Freelance Web Developer</span>
        <h1 class="headline">I build clean, fast, responsive web apps.</h1>
        <p class="sub">
          Crafting delightful user experiences with modern HTML, CSS, and JavaScript.
          Available for landing pages, dashboards, and full-stack prototypes.
        </p>
        <div class="cta-row">
          <a class="btn primary" href="#projects">View Projects</a>
          <a class="btn" href="#contact">Hire Me</a>
        </div>
        <div style="margin-top:1rem" class="hint">Based in India • Remote-friendly • Typical response within 24h</div>
      </div>
      <div class="card">
        <h3 style="margin:.25rem 0 0">Quick Stats</h3>
        <div class="grid" style="grid-template-columns:repeat(3,1fr); margin-top:.75rem">
          <div class="card" style="padding:.8rem">
            <div class="pill">+5 yrs</div>
            <div>Experience</div>
          </div>
          <div class="card" style="padding:.8rem">
            <div class="pill">30+</div>
            <div>Projects</div>
          </div>
          <div class="card" style="padding:.8rem">
            <div class="pill">100%</div>
            <div>Remote</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- About -->
  <section id="about">
    <div class="container grid about-wrap reveal">
      <div class="card">
        <h2>About Me</h2>
        <p class="lead">
          I’m Roja, a freelance developer focused on performant, accessible, and maintainable web
          interfaces. I partner with startups and SMEs to ship features quickly without sacrificing quality.
        </p>
        <p>
          My toolkit includes semantic HTML, modern CSS (Grid/Flexbox), vanilla JS and frameworks,
          API integrations, and build tooling. I care about good DX and clean design systems.
        </p>
      </div>
      <div class="grid">
        <div class="card">
          <h3>What I Do</h3>
          <ul style="margin:.25rem 0 0; padding-left:1rem; line-height:1.7">
            <li>Landing pages that convert</li>
            <li>UI components & design systems</li>
            <li>Dashboards and admin panels</li>
            <li>Performance audits & refactors</li>
          </ul>
        </div>
        <div class="card">
          <h3>How I Work</h3>
          <ul style="margin:.25rem 0 0; padding-left:1rem; line-height:1.7">
            <li>Clear communication, async-friendly</li>
            <li>Short feedback cycles with previews</li>
            <li>Tested, accessible, documented</li>
            <li>Own the outcome, not just tasks</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Skills -->
  <section id="skills">
    <div class="container reveal">
      <h2>Skills</h2>
      <p class="lead">A flexible stack for modern web apps.</p>
      <div class="chips">
        <span class="chip">HTML5</span><span class="chip">CSS3 / Grid / Flexbox</span>
        <span class="chip">JavaScript (ES6+)</span><span class="chip">TypeScript</span>
        <span class="chip">React</span><span class="chip">Node / Express</span>
        <span class="chip">REST / JSON</span><span class="chip">Fetch / Axios</span>
        <span class="chip">Responsive Design</span><span class="chip">Accessibility (a11y)</span>
        <span class="chip">Git / GitHub</span><span class="chip">Vite</span>
      </div>
    </div>
  </section>

  <!-- Projects -->
  <section id="projects">
    <div class="container reveal">
      <h2>Projects</h2>
      <p class="lead">Selected work. Click a card to see details.</p>

      <div class="grid projects-grid">
        <!-- Project 1 -->
        <article class="project" tabindex="0" data-project='{
          "title":"SaaS Landing",
          "desc":"High-converting landing page with pricing table, testimonials, and analytics events.",
          "stack":["HTML","CSS","JavaScript"],
          "link":"https://example.com"
        }'>
          <div class="pill">Marketing</div>
          <h3>SaaS Landing</h3>
          <p>Responsive landing with 90+ Lighthouse scores.</p>
          <div class="tags">
            <span class="pill">HTML</span><span class="pill">CSS</span><span class="pill">JS</span>
          </div>
        </article>

        <!-- Project 2 -->
        <article class="project" tabindex="0" data-project='{
          "title":"E-Commerce UI",
          "desc":"Product grid, filters, cart drawer, and checkout mock using local storage.",
          "stack":["HTML","CSS","JavaScript"],
          "link":"https://example.com"
        }'>
          <div class="pill">E-Commerce</div>
          <h3>E-Commerce UI</h3>
          <p>Clean storefront with cart & persistence.</p>
          <div class="tags">
            <span class="pill">Grid</span><span class="pill">Filters</span><span class="pill">LocalStorage</span>
          </div>
        </article>

        <!-- Project 3 -->
        <article class="project" tabindex="0" data-project='{
          "title":"Blog Platform",
          "desc":"Content-focused blog with categories and dark themed typography.",
          "stack":["HTML","CSS","JavaScript"],
          "link":"https://example.com"
        }'>
          <div class="pill">Content</div>
          <h3>Blog Platform</h3>
          <p>Accessible blog with search and tags.</p>
          <div class="tags">
            <span class="pill">Search</span><span class="pill">Tags</span><span class="pill">A11y</span>
          </div>
        </article>
      </div>
    </div>
  </section>

  <!-- Contact -->
  <section id="contact">
    <div class="container grid about-wrap reveal">
      <div class="card">
        <h2>Contact</h2>
        <p class="lead">Tell me about your project. I’ll get back within one business day.</p>
        <form id="contactForm" novalidate>
          <div class="grid" style="grid-template-columns:1fr 1fr">
            <input id="name" name="name" placeholder="Your name" required />
            <input id="email" name="email" type="email" placeholder="Your email" required />
          </div>
          <input id="subject" name="subject" placeholder="Subject (optional)" />
          <textarea id="message" name="message" placeholder="Project details" required></textarea>
          <button class="btn primary" type="submit">Send Message</button>
          <div id="formMessage" class="hint" role="status" aria-live="polite"></div>
        </form>
      </div>
      <div class="card">
        <h3>Availability</h3>
        <p>Booking <strong>new projects</strong> for this month. Short engagements welcome.</p>
        <div style="height:1px; background:var(--line); margin:1rem 0"></div>
        <h3>Elsewhere</h3>
        <p><a href="#" onclick="alert('Replace with your GitHub URL')">GitHub</a> •
           <a href="#" onclick="alert('Replace with your LinkedIn URL')">LinkedIn</a> •
           <a href="mailto:you@example.com">you@example.com</a></p>
      </div>
    </div>
  </section>

  <footer>
    <div class="container">
      © <span id="year"></span> Roja — Freelance Developer. Built with vanilla HTML, CSS, and JS.
    </div>
  </footer>

  <!-- Modal -->
  <dialog id="projectModal" style="border:none; border-radius:16px; padding:0; max-width:560px; width:92%">
    <div class="card" style="border:none; border-radius:16px">
      <div style="display:flex; justify-content:space-between; align-items:center">
        <h3 id="mTitle" style="margin:.25rem 0"></h3>
        <button id="mClose" class="btn">Close</button>
      </div>
      <p id="mDesc" class="lead" style="margin:.5rem 0 1rem"></p>
      <div id="mStack" class="chips" style="margin-bottom:1rem"></div>
      <a id="mLink" class="btn primary" target="_blank" rel="noopener">Open Project</a>
    </div>
  </dialog>

  <script>
    // Mobile nav toggle
    const menuBtn = document.getElementById('menuBtn');
    const navLinks = document.getElementById('navLinks');
    menuBtn.addEventListener('click', ()=> navLinks.classList.toggle('show'));
    // Close menu on link click (mobile)
    navLinks.querySelectorAll('a').forEach(a => a.addEventListener('click', ()=> navLinks.classList.remove('show')));

    // Year
    document.getElementById('year').textContent = new Date().getFullYear();

    // Reveal on scroll
    const io = new IntersectionObserver((entries)=>{
      entries.forEach(e=>{
        if(e.isIntersecting){ e.target.classList.add('show'); io.unobserve(e.target); }
      })
    },{threshold:.12});
    document.querySelectorAll('.reveal').forEach(el=> io.observe(el));

    // Project modal
    const modal = document.getElementById('projectModal');
    const mTitle = document.getElementById('mTitle');
    const mDesc  = document.getElementById('mDesc');
    const mStack = document.getElementById('mStack');
    const mLink  = document.getElementById('mLink');
    const mClose = document.getElementById('mClose');

    function openProject(el){
      const data = JSON.parse(el.getAttribute('data-project') || '{}');
      mTitle.textContent = data.title || 'Project';
      mDesc.textContent  = data.desc  || '';
      mStack.innerHTML   = (data.stack||[]).map(s=> `<span class="chip">${s}</span>`).join('');
      mLink.href         = data.link || '#';
      modal.showModal();
    }

    document.querySelectorAll('.project').forEach(card=>{
      card.addEventListener('click', ()=> openProject(card));
      card.addEventListener('keypress', (e)=>{ if(e.key==='Enter') openProject(card) });
    });
    mClose.addEventListener('click', ()=> modal.close());
    modal.addEventListener('click', (e)=>{ if(e.target===modal) modal.close() });

    // Contact form (client-side)
    const form = document.getElementById('contactForm');
    const msg  = document.getElementById('formMessage');

    form.addEventListener('submit', (e)=>{
      e.preventDefault();
      const name = form.name.value.trim();
      const email = form.email.value.trim();
      const message = form.message.value.trim();

      if(!name || !email || !message){
        msg.textContent = 'Please fill out all required fields.';
        msg.style.color = '#ffb3b3';
        return;
      }
      // very basic email check
      const okEmail = /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
      if(!okEmail){
        msg.textContent = 'Please enter a valid email address.';
        msg.style.color = '#ffb3b3';
        return;
      }

      // Simulate send
      msg.textContent = 'Thanks! Your message has been queued. I will reply shortly.';
      msg.style.color = '#b8f3e9';
      form.reset();

      // If you want real email delivery, connect to a backend or a service like EmailJS/Formspree here.
      // Example (Formspree):
      // fetch('https://formspree.io/f/xxxxxx',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,message})})
      //   .then(()=>{ msg.textContent='Message sent successfully!'; })
      //   .catch(()=>{ msg.textContent='Something went wrong. Please email me directly.'; });
    });
  </script>
</body>
</html>
